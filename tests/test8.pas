Program test8

(*This tests nested loops. It will print every number from 1.0
  to the real number n you input n times*)

var
x, z : real;

begin
    readin(x);
    for z := 1.0 to x do
    begin
        while i <= x do
        var
        i : real = 1.0;
        begin
            writeln(i);
            i := i + 1.0;
        end;
    end;
end.